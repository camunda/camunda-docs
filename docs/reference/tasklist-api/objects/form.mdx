---
id: form
title: Form
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Describes task embedded form

```graphql
type Form {
  id: String!
  processDefinitionId: String!
  schema: String!
}
```

### Fields

#### `id` ([`String!`](/docs/reference/tasklist-api/scalars/string))

The unique identifier of the embedded form within one process

#### `processDefinitionId` ([`String!`](/docs/reference/tasklist-api/scalars/string))

Reference to process definition

#### `schema` ([`String!`](/docs/reference/tasklist-api/scalars/string))

Form content
