---
id: current-user
title: currentUser
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Get currently logged in user.

```graphql
currentUser: User!

```

### Type

#### [`User`](/docs/reference/tasklist-api/objects/user)

Describes the user.
