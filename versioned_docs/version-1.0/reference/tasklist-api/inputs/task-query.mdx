---
id: task-query
title: TaskQuery
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Task query - query to get one page of tasks.

```graphql
type TaskQuery {
  state: TaskState
  assigned: Boolean
  assignee: String
  pageSize: Int
  searchAfter: [String!]
  searchAfterOrEqual: [String!]
  searchBefore: [String!]
  searchBeforeOrEqual: [String!]
}
```

### Fields

#### `state` ([`TaskState`](/reference/tasklist-api/enums/task-state.mdx))

State of the tasks

#### `assigned` ([`Boolean`](/reference/tasklist-api/scalars/boolean.mdx))

Are the tasks assigned?

#### `assignee` ([`String`](/reference/tasklist-api/scalars/string.mdx))

Who is assigned to the tasks?

#### `pageSize` ([`Int`](/reference/tasklist-api/scalars/int.mdx))

Size of tasks page (default: 50).

#### `searchAfter` ([`[String!]`](/reference/tasklist-api/scalars/string.mdx))

Array of values copied from `sortValues` of one of the tasks, query will return page of tasks going directly after this values plus same sort values.

#### `searchAfterOrEqual` ([`[String!]`](/reference/tasklist-api/scalars/string.mdx))

Array of values copied from `sortValues` of one of the tasks, query will return page of tasks going directly after this values.

#### `searchBefore` ([`[String!]`](/reference/tasklist-api/scalars/string.mdx))

Array of values copied from `sortValues` of one of the tasks, query will return page of tasks going directly before this values plus same sort values.

#### `searchBeforeOrEqual` ([`[String!]`](/reference/tasklist-api/scalars/string.mdx))

Array of values copied from `sortValues` of one of the tasks, query will return page of tasks going directly before this values.
