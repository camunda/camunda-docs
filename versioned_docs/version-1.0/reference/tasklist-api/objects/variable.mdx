---
id: variable
title: Variable
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

Variable used in task.

```graphql
type Variable {
  name: String!
  value: String!
}
```

### Fields

#### `name` ([`String!`](/reference/tasklist-api/scalars/string.mdx))

#### `value` ([`String!`](/reference/tasklist-api/scalars/string.mdx))
