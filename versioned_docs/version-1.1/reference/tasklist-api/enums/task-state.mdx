---
id: task-state
title: TaskState
---

export const Tag = ({children, color}) => (
<span
style={{
      backgroundColor: color,
      borderRadius: '2px',
      color: '#fff',
      padding: '0.2rem',
    }}>
{children}
</span>
);

State of the task.

```graphql
enum TaskState {
  CREATED
  COMPLETED
  CANCELED
}
```

### Values

#### `CREATED`

#### `COMPLETED`

#### `CANCELED`
